name: Edge User Probe
on:
  push:
  pull_request:

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Check Supabase Edge user visibility
        run: |
          set -euo pipefail
          curl -sS --connect-timeout 10 --max-time 20 \
            -w '\nHTTP %{http_code}\n' \
            https://iqrnytsgwaiegddfxfjs.supabase.co/functions/v1/gestao-ads-user-probe
